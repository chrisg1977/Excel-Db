-- 1) App user: can read/write data, cannot ALTER/CREATE/DROP schema
CREATE USER app_directus WITH PASSWORD 'CHANGE_ME_STRONG';

-- 2) Schema admin: used only when we intentionally change tables/views
CREATE USER schema_admin WITH PASSWORD 'CHANGE_ME_STRONG_2';