      width: min(1320px, 100%);
      height: 100%;
      background: #fff;
      box-shadow: -8px 0 24px rgba(0,0,0,0.18);
      overflow: auto;
      padding: 22px;
      display: grid;
      grid-template-columns: minmax(320px, 420px) minmax(0, 1fr);
      grid-template-rows: auto auto;
      gap: 20px;
      background:
        radial-gradient(circle at top left, rgba(80,120,255,0.08), transparent 28%),
        linear-gradient(180deg, #ffffff 0%, #f8fbff 100%);
      border-left: 10px solid #ffb703;
    }
    .issue-left,
    .issue-right {
      display: grid;
      gap: 14px;
      align-content: start;
    }
    .issue-left {
      padding-right: 8px;
      border-right: 1px solid #e4ebf5;
    }
    .issue-right {
      padding-left: 4px;
    }
    .issue-bottom {
      grid-column: 1 / -1;
      display: grid;
      grid-template-columns: minmax(0, 1fr) 220px;
      gap: 16px;
      align-items: start;
    }
    .issue-hero {
      position: relative;
      border: 1px solid #cfdcf1;
      border-radius: 18px;
      background: linear-gradient(135deg, #173a73 0%, #2f5fa8 55%, #7aa3db 100%);
      color: #fff;
      padding: 16px 18px 18px;
      box-shadow: 0 14px 32px rgba(35, 64, 111, 0.18);
      display: grid;
      grid-template-columns: 96px minmax(0, 1fr);
      gap: 14px 16px;
      align-items: start;
    }
    .issue-hero.main {
      background: linear-gradient(135deg, #157347 0%, #1f9d60 55%, #57c785 100%);
      border-color: #a9dfc1;
    }
    .issue-hero.provider {
      background: linear-gradient(135deg, #173a73 0%, #2f5fa8 55%, #7aa3db 100%);
      border-color: #b9cee9;
    }
    .issue-hero.o3p {
      background: linear-gradient(135deg, #6b3f15 0%, #8f5a23 55%, #c59249 100%);
      border-color: #e2c08d;
    }
    .issue-nav {
      display: flex;
      gap: 8px;
      align-items: center;
      flex-wrap: wrap;
    }
    .issue-form-card,
    .issue-year-card,
    .issue-note-card {
      border: 1px solid var(--line);
      border-radius: 14px;
      background: #fbfcff;
      padding: 14px;
      display: grid;
      gap: 8px;
    }
    .issue-payment-card {
      border: 1px solid var(--line);
      border-radius: 14px;
      background: linear-gradient(180deg, #ffffff 0%, #f7faff 100%);
      padding: 14px;
      display: grid;
      gap: 10px;
    }
    .issue-payment-layout {
      display: grid;
      grid-template-columns: minmax(220px, 0.95fr) minmax(220px, 1.05fr);
      gap: 14px;
      align-items: start;
    }
    .issue-payment-grid {
      display: grid;
      grid-template-columns: minmax(0, 1fr);
      gap: 10px;
    }
    .issue-payment-meta {
      display: grid;
      gap: 6px;
      font-size: 12px;
      color: #5b6b84;
    }
    .issue-payment-meta strong {
      color: #173a73;
      font-size: 12px;
      letter-spacing: 0.04em;
      text-transform: uppercase;
    }
    .issue-payment-action {
      display: grid;
      gap: 10px;
      align-content: start;
    }
    .issue-payment-action .btn {
      width: 100%;
      justify-content: center;
    }
    .issue-form-card label {
      display: grid;
      gap: 4px;
    }
    .issue-form-card input {
      border: 1px solid #cfd7e3;
      border-radius: 6px;
      padding: 8px 10px;
      font-size: 14px;
      background: #fff;
    }
    .issue-warning {
      border: 1px solid #ffd39c;
      background: #fff7ed;
      color: #9a3412;
      border-radius: 6px;
      padding: 8px 10px;
      font-size: 12px;
      font-weight: 700;
    }
    .issue-warning[hidden] { display: none; }
    .issue-totals {
      display: grid;
      gap: 6px;
      font-size: 13px;
    }
    .issue-total-row {
      display: flex;
      justify-content: space-between;
      gap: 10px;
      font-weight: 700;
    }
    .year-view-table {
      width: 100%;
      border-collapse: collapse;
      min-width: 0;
      font-size: 12px;
    }
    .year-view-table th,
    .year-view-table td {
      border-bottom: 1px solid #edf0f5;
      padding: 6px 5px;
    }
    .year-view-table th {
      position: static;
      background: #eef4ff;
      color: #23406f;
    }
    .issue-actions {
      display: flex;
      gap: 8px;
      flex-wrap: wrap;
    }
    .issue-review-toolbar {
      display: flex;
      gap: 10px;
      flex-wrap: wrap;
      align-items: center;
      border: 1px solid #d7e3f5;
      border-radius: 14px;
      padding: 12px;
      background: linear-gradient(180deg, #f8fbff 0%, #eef4ff 100%);
    }
    .issue-hero-main {
      display: grid;
      gap: 3px;
      min-width: 0;
    }
    .issue-hero-topbar {
      grid-column: 1 / -1;
      display: flex;
      justify-content: space-between;
      align-items: center;
      gap: 8px;
      padding: 10px 12px;
      margin: -2px -4px 6px;
      border-radius: 14px;
      background: rgba(12, 23, 42, 0.26);
      border: 1px solid rgba(255,255,255,0.16);
      backdrop-filter: blur(4px);
    }
    .issue-hero-tabs {
      display: flex;
      gap: 8px;
      flex-wrap: nowrap;
      overflow: hidden;
      min-width: 0;
    }
    .issue-hero-tabs .btn {
      min-width: 88px;
      border-radius: 12px 12px 0 0;
      border-color: rgba(255,255,255,0.35);
      background: rgba(255,255,255,0.10);
      color: #fff;
      margin-bottom: -8px;
    }
    .issue-hero-tabs .btn.active {
      background: rgba(255,255,255,0.22);
      border-color: rgba(255,255,255,0.55);
      box-shadow: 0 -1px 0 rgba(255,255,255,0.25), inset 0 1px 0 rgba(255,255,255,0.35);
    }
    .issue-hero-tabs .btn.tab-main { background: rgba(21, 115, 71, 0.24); border-color: rgba(173, 230, 196, 0.45); }
    .issue-hero-tabs .btn.tab-provider { background: rgba(31, 94, 168, 0.34); border-color: rgba(183, 208, 243, 0.45); }
    .issue-hero-tabs .btn.tab-o3p { background: rgba(143, 90, 35, 0.32); border-color: rgba(226, 192, 141, 0.45); }
    .issue-hero-tabs .btn.tab-main.active { background: #dff7eb; color: #0f6a3b; border-color: #93dbb2; }
    .issue-hero-tabs .btn.tab-provider.active { background: #4d8fe2; color: #ffffff; border-color: #b7d0f3; }
    .issue-hero-tabs .btn.tab-o3p.active { background: #c79249; color: #3b2208; border-color: #e2c08d; }
    .issue-avatar-wrap {
      display: grid;
      gap: 10px;
      justify-items: stretch;
    }
    .issue-hero-name-line {
      font-size: 26px;
      font-weight: 900;
      letter-spacing: -0.03em;
      line-height: 1;
      color: #fff;
      white-space: normal;
      overflow-wrap: anywhere;
      max-width: 100%;
    }
    .issue-hero-empid {
      font-size: 13px;
      color: rgba(255,255,255,0.9);
      font-weight: 700;
    }
    .issue-hero-role {
      font-size: 14px;
      color: rgba(255,255,255,0.92);
      font-weight: 700;
    }
    .issue-hero-queue {
      margin-top: 8px;
      justify-content: flex-start;
    }
    .issue-hero-queue .btn {
      color: #fff;
      border-color: rgba(255,255,255,0.28);
      background: rgba(255,255,255,0.08);
    }
    .issue-hero-close {
      position: static;
      z-index: 2;
      color: #fff;
      border-color: rgba(255,255,255,0.3);
      background: rgba(255,255,255,0.08);
    }
    .issue-hero-progress {
      position: absolute;
      right: 18px;
      bottom: 16px;
      font-size: 12px;
      font-weight: 900;
      color: rgba(255,255,255,0.92);
      letter-spacing: 0.04em;
      text-transform: uppercase;
    }
    .issue-review-toolbar .btn {
      min-width: 132px;
    }
    .issue-final-actions {
      display: grid;
      gap: 10px;
      border: 1px solid #d7e3f5;
      border-radius: 14px;
      padding: 14px;
      background: linear-gradient(180deg, #f8fbff 0%, #eef4ff 100%);
    }
    .issue-final-actions .btn {
      width: 100%;
      justify-content: center;
    }
    .issue-note-list {
      margin: 0;
      padding-left: 18px;
      color: #7c2d12;
    }
    .issue-adjustment-stack {
      display: grid;
      gap: 10px;
    }
    .issue-adjustment-card {
      border: 1px solid #e3ebf6;
      border-radius: 12px;
      background: #fff;
      padding: 10px;
      display: grid;
      gap: 8px;
    }
    .issue-adjustment-card strong {
      color: #173a73;
      font-size: 12px;
      text-transform: uppercase;
      letter-spacing: 0.06em;
    }
    .issue-action-card {
      border-left: 4px solid #c9d8ee;
      background: linear-gradient(180deg, #ffffff 0%, #f9fbff 100%);
    }
    .issue-adjustment-grid {
      display: grid;
      grid-template-columns: repeat(2, minmax(0, 1fr));
      gap: 8px;
    }
    .issue-adjustment-grid.compact-single {
      grid-template-columns: minmax(0, 220px);
    }
    .issue-adjustment-card label {
      display: grid;
      gap: 3px;
      font-size: 11px;
      color: #516073;
    }
    .issue-adjustment-card input,
    .issue-adjustment-card select,
    .issue-adjustment-card textarea {
      border: 1px solid #cfd7e3;
      border-radius: 8px;
      padding: 8px 10px;
      font-size: 13px;
      background: #fff;
      font-family: inherit;
    }
    .issue-adjustment-card textarea {
      min-height: 72px;
      resize: vertical;
    }
    .issue-adjustment-note {
      font-size: 12px;
      color: #6b7280;
    }
    .allocation-editor {
      display: grid;
      gap: 6px;
    }
    .allocation-row {
      display: grid;
      grid-template-columns: 76px minmax(146px, 1fr) minmax(130px, 1fr) minmax(124px, auto) auto;
      gap: 6px;
      align-items: end;
    }
    .allocation-row[hidden] {
      display: none;
    }
    .allocation-row .btn {
      min-width: 42px;
    }
    .allocation-preview {
      font-size: 11px;
      color: #173a73;
      font-weight: 700;
      align-self: center;
      white-space: nowrap;
    }
    .allocation-row.selective-remove .btn[data-field="remove"] {
      visibility: hidden;
    }
    .allocation-total {
      font-size: 12px;
      color: #516073;
      font-weight: 700;
    }
    .loan-decision {
      display: grid;
      gap: 6px;
    }
    .loan-button-row {
      display: flex;
      gap: 6px;
      flex-wrap: wrap;
    }
    .loan-button-row .btn.active {
      background: #173a73;
      border-color: #173a73;
      color: #fff;
    }
    .issue-preview-note {
      border-top: 1px dashed #d4dfef;
      padding-top: 10px;
      font-size: 13px;
      color: #374151;
    }
    .preview-sheet {
      border: 1px solid #d6e1f1;
      border-radius: 16px;
      background: linear-gradient(180deg, #fefefe 0%, #f7fbff 100%);
      padding: 16px;
      display: grid;
      gap: 14px;
      box-shadow: inset 0 1px 0 rgba(255,255,255,0.75);
    }
    .preview-sheet-header {
      display: flex;
      justify-content: space-between;
      gap: 14px;
      align-items: flex-start;
      border-bottom: 1px solid #dde7f4;
      padding-bottom: 12px;
    }
    .preview-sheet-title {
      font-size: 20px;
      font-weight: 900;
      color: #173a73;
    }
    .preview-sheet-sub {
      font-size: 12px;
      color: #5f6f86;
      margin-top: 4px;
    }
    .preview-sheet-tag {
      border: 1px solid #c5d4ec;
      border-radius: 999px;
      padding: 6px 10px;
      font-size: 11px;
      font-weight: 800;
      color: #173a73;
      background: #eef4ff;
      white-space: nowrap;
    }
    .preview-sheet-grid {
      display: grid;
      grid-template-columns: minmax(0, 1.05fr) minmax(260px, 0.95fr);
      gap: 14px;
      align-items: start;
    }
    .preview-sheet-section {
      border: 1px solid #dde7f4;
      border-radius: 14px;
      background: #fff;
      overflow: hidden;
    }
    .preview-section-head {
      background: linear-gradient(180deg, #edf4ff 0%, #e4eeff 100%);
      color: #173a73;
      padding: 10px 12px;
      font-size: 12px;
      font-weight: 900;
      text-transform: uppercase;
      letter-spacing: 0.08em;
    }
    .preview-rows {
      display: grid;
    }
    .preview-row {
      display: grid;
      grid-template-columns: minmax(0, 1fr) auto;
      gap: 12px;
      padding: 9px 12px;
      border-top: 1px solid #edf2f8;
      align-items: center;
      font-size: 13px;
    }
    .preview-row:first-child {
      border-top: none;
    }
    .preview-row strong {
      color: #1e293b;
      font-size: 12px;
      text-transform: uppercase;
      letter-spacing: 0.03em;
    }
    .preview-row span:last-child {
      font-weight: 800;
      color: #173a73;
    }
    .preview-row.is-total {
      background: #f7fbff;
    }
    .preview-row.is-total span:last-child {
      font-size: 16px;
      color: #0f2f61;
    }
    .preview-row[hidden] {
      display: none;
    }
    .preview-sidebar {
      display: grid;
      gap: 12px;
    }
    .preview-sidebar-card {
      border: 1px solid #dde7f4;
      border-radius: 14px;
      background: #fff;
      padding: 12px;
      display: grid;
      gap: 10px;
    }
    .preview-sidebar-card strong {
      color: #173a73;
      font-size: 12px;
      letter-spacing: 0.06em;
      text-transform: uppercase;
    }
    .preview-sidebar-grid {
      display: grid;
      grid-template-columns: repeat(2, minmax(0, 1fr));
      gap: 8px;
    }
    .preview-side-metric {
      border: 1px solid #edf2f8;
      border-radius: 12px;
      padding: 10px;
      background: #f8fbff;
      display: grid;
      gap: 4px;
    }
    .preview-side-metric span {
      font-size: 11px;
      color: #5f6f86;
      text-transform: uppercase;
      letter-spacing: 0.04em;
    }
    .preview-side-metric strong {
      font-size: 16px;
      color: #102c59;
      text-transform: none;
      letter-spacing: 0;
    }
    .preview-footer-note {
      border-top: 1px dashed #d7e3f5;
      padding-top: 10px;
      color: #5f6f86;
      font-size: 12px;
    }
    .payslip-card {
      border: 1px solid #d2dfef;
      border-radius: 18px;
      background: linear-gradient(180deg, #ffffff 0%, #f6faff 100%);
      box-shadow: inset 0 1px 0 rgba(255,255,255,0.85), 0 18px 40px rgba(15, 23, 42, 0.06);
      overflow: hidden;
      display: grid;
    }
    .payslip-card-top {
      display: grid;
      grid-template-columns: 112px minmax(0, 1.35fr) minmax(180px, 1fr);
      border-bottom: 1px solid #dbe5f2;
      background: linear-gradient(180deg, #e7f1ff 0%, #dcecff 100%);
    }
    .payslip-brand {
      padding: 16px 12px;
      border-right: 1px solid #c8d9ef;
      display: grid;
      align-content: center;
      gap: 4px;
      background: linear-gradient(180deg, #f8fbff 0%, #eef5ff 100%);
    }
    .payslip-brand strong {
      font-size: 18px;
      color: #0f4c81;
      font-weight: 900;
      letter-spacing: -0.03em;
    }
    .payslip-brand span {
      font-size: 11px;
      color: #6b7b92;
      text-transform: uppercase;
      letter-spacing: 0.08em;
    }
    .payslip-identity {
      padding: 14px 16px;
      display: grid;
      grid-template-columns: minmax(0, 1fr) auto auto;
      gap: 8px 16px;
      align-items: start;
    }
    .payslip-identity .name-stack {
      display: grid;
      gap: 2px;
      min-width: 0;
    }
    .payslip-surname {
      font-size: 24px;
      line-height: 1;
      font-weight: 900;
      color: #0f2447;
      overflow-wrap: anywhere;
    }
    .payslip-given {
      font-size: 18px;
      line-height: 1.1;
      font-weight: 800;
      color: #173a73;
      overflow-wrap: anywhere;
    }
    .payslip-meta-box {
      display: grid;
      gap: 3px;
      align-content: start;
      justify-items: end;
      min-width: 78px;
    }
    .payslip-meta-box span {
      font-size: 11px;
      text-transform: uppercase;
      letter-spacing: 0.06em;
      color: #607089;
    }
    .payslip-meta-box strong {
      font-size: 22px;
      color: #0f2447;
      font-weight: 900;
    }
    .payslip-work-profile {
      padding: 14px 16px;
      border-left: 1px solid #c8d9ef;
      display: grid;
      gap: 10px;
      background: linear-gradient(180deg, #edf5ff 0%, #e5effd 100%);
    }
    .payslip-profile-row {
      display: grid;
      grid-template-columns: repeat(4, minmax(0, 1fr));
      gap: 8px;
    }
    .payslip-profile-cell {
      display: grid;
      gap: 3px;
    }
    .payslip-profile-cell span {
      font-size: 10px;
      text-transform: uppercase;
      letter-spacing: 0.08em;
      color: #607089;
    }
    .payslip-profile-cell strong {
      font-size: 15px;
      font-weight: 900;
      color: #102a4d;
    }
    .payslip-gross-hero {
      padding: 14px 16px;
      display: flex;
      justify-content: space-between;
      align-items: end;
      gap: 12px;
      background: linear-gradient(180deg, #dcecff 0%, #cfe3ff 100%);
      border-top: 1px solid #c8d9ef;
    }
    .payslip-gross-hero span {
      font-size: 12px;
      text-transform: uppercase;
      letter-spacing: 0.08em;
      color: #173a73;
      font-weight: 800;
    }
    .payslip-gross-hero strong {
      font-size: 20px;
      font-weight: 900;
      color: #081a38;
    }
    .payslip-card-body {
      display: grid;
      gap: 12px;
      padding: 14px;
    }
    .payslip-section {
      border: 1px solid #dde7f4;
      border-radius: 14px;
      background: #fff;
      overflow: hidden;
    }
    .payslip-section-head {
      background: linear-gradient(180deg, #edf4ff 0%, #e4eeff 100%);
      color: #173a73;
      padding: 9px 12px;
      font-size: 11px;
      font-weight: 900;
      text-transform: uppercase;
      letter-spacing: 0.08em;
    }
    .payslip-line-list {
      display: grid;
    }
    .payslip-line {
      display: grid;
      grid-template-columns: minmax(0, 1.2fr) auto auto;
      gap: 12px;
      align-items: center;
      padding: 10px 12px;
      border-top: 1px solid #edf2f8;
    }
    .payslip-line:first-child {
      border-top: none;
    }
    .payslip-line-main {
      display: grid;
      gap: 2px;
      min-width: 0;
    }
    .payslip-line-main strong {
      font-size: 12px;
      text-transform: uppercase;
      letter-spacing: 0.05em;
      color: #173a73;
    }
    .payslip-line-main span {
      font-size: 11px;
      color: #6b7280;
    }
    .payslip-line-hours {
      font-size: 12px;
      font-weight: 800;
      color: #334155;
      white-space: nowrap;
      text-align: right;
      min-width: 66px;
    }
    .payslip-line-amount {
      font-size: 14px;
      font-weight: 900;
      color: #111827;
      text-align: right;
      white-space: nowrap;
      min-width: 92px;
    }
    .payslip-line[hidden] {
      display: none;
    }
    .payslip-statutory-grid {
      display: grid;
      grid-template-columns: repeat(5, minmax(0, 1fr));
      border-top: 1px solid #edf2f8;
    }
    .payslip-stat {
      padding: 12px;
      border-left: 1px solid #edf2f8;
      display: grid;
      gap: 4px;
    }
    .payslip-stat:first-child {
      border-left: none;
    }
    .payslip-stat span {
      font-size: 10px;
      color: #607089;
      text-transform: uppercase;
      letter-spacing: 0.07em;
    }
    .payslip-stat strong {
      font-size: 16px;
      font-weight: 900;
      color: #0f2447;
    }
